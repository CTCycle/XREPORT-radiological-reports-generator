import { Component } from '@angular/core';

@Component({ selector: 'app-inference-page', template: '<main class="page-shell"><p class="eyebrow">Inference</p><h1>Turn a radiograph into a draft report</h1><p>Select a local model, provide the study, then review the generated text before it leaves the workspace.</p></main>' })
export class InferencePage {}
