# S13 SQLite migration and restart persistence — 2026-09-23

## Scope and revision

S13 passed for the isolated SQLite storage slice. The application source revision at execution was `fdb4a8bf9af37eadea648851cb1a0b54e187f7b3`; the focused SQLite foreign-key test was committed in validation revision `906b455d90c4a938704a8a62d843455d75ab41fe`, which passed hosted [CI run 35834323975](https://github.com/CTCycle/XREPORT-radiological-reports-generator/actions/runs/35834323975). No production schema or API changes were made. The representative history record is a synthetic fixture and does not involve model download or inference generation.

## Isolated restart scenario

- Resource root: `runtimes/cache/validation-s13-20260923` (created for this run; existing application data was not used).
- Database: SQLite under that resource root; configured Alembic head and the database's `alembic_version` both reported `e91a4f6c2d73`.
- The official launcher preflight declined to start on alternate settings while the existing XREPORT services still owned ports 5003 and 8003. Their PIDs and executable paths were checked; they were left running. To keep the S13 database isolated, the backend was started directly on `127.0.0.1:5013` with `XREPORT_RESOURCES_DIR` set to the isolated root and `EMBEDDED_DATABASE=true`.
- Before stopping the first backend process, `PATCH /api/settings` stored `global.seed=424242`. A synthetic successful history run was created with request ID `s13-restart-20260923`, image `s13-fixture.png`, and one linked report whose generated text was `Findings\nSynthetic persistence fixture.`
- The backend process was stopped and relaunched against the same resource root. Startup reported the database already at the Alembic head. After restart, `GET /api/settings` returned seed `424242`; `GET /api/inference/history/s13-restart-20260923` returned status `succeeded` and the same linked image/report text.
- Health, settings, and history API reads returned HTTP 200 in both lifecycles. Both temporary backend processes were stopped, and port 5013 was confirmed clear. No migration was applied during the second launch.

## Automated evidence

The focused database, settings, and repository suites were run with the existing `app/server/.venv` and repository-local pytest paths:

```powershell
& 'app/server/.venv/Scripts/python.exe' -m pytest -c app/server/pyproject.toml `
  app/tests/unit/test_database_initialization.py `
  app/tests/unit/test_application_settings.py `
  app/tests/unit/test_repository_persistence.py `
  -q --basetemp runtimes/cache/pytest-tmp/s13-sqlite-20260923-push-check `
  -o 'cache_dir=runtimes/cache/pytest'
```

Result: `27 passed in 3.09s`. This includes repeatable migration to head, fail-closed rejection of unversioned and unknown partial schemas, injected migration rollback, and head-schema drift checks.

New focused test: `test_sqlite_enforces_foreign_keys_and_cascades_report_deletion` in `app/tests/unit/test_database_initialization.py`. It verifies `PRAGMA foreign_keys=1`, proves an orphan `InferenceReport` insert raises `IntegrityError`, and confirms deleting an `InferenceRun` through SQL removes its child report row. Ruff also passed on the modified database and E2E test files.

## Boundaries

This covers local SQLite initialization, the specified setting and one linked history record across a backend restart, the current migration head, fail-closed/rollback regressions, and SQLite foreign-key enforcement. It does not cover the official launcher's occupied-port recovery, model execution, PostgreSQL migrations/restart behavior, or other S13 workflows beyond the scenarios listed above. The isolated database and task-specific pytest basetemps were removed after the evidence was captured.
