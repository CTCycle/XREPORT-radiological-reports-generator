# S43 Windows launcher maintenance validation

Initial validation on 2026-09-25 used checkout 7d8eb47165258bc0d994b3946d75a69940fbf816. The ClearCache fix was rerun against the working-tree launcher after that baseline.

Scope followed [runtime startup and maintenance guidance](../../../docs/runtime/startup.md) and inspected the start_on_windows.ps1 actions ClearCache, RemoveCheckpoints, RemoveAllData, RemoveDesktopRelease, and KillProcesses. Destructive data scenarios used only the disposable resource root under this evidence directory. The source application on ports 5003/8003 was owned by the coordinating task and was left running and untouched.

| Action | Result | Evidence and limit |
| --- | --- | --- |
| RemoveCheckpoints | PASS for isolated fixture behavior | Invoked the real launcher action with XREPORT_RESOURCES_DIR set to the fixture. It removed the one synthetic checkpoint and retained the checkpoint directory and .gitkeep, database and WAL, model and tokenizer, log, .env, and template. The launcher exited 0. |
| RemoveAllData | PASS for isolated fixture behavior | Invoked the real launcher action against that disposable root. It removed the database and sidecar, model/tokenizer, checkpoint content, and log; preserved the resource root, .gitkeep, .env, and template; exited 0 after removing 6 items. A repeated action exited 0 and reported 0 items. |
| ClearCache | FIXED AND REVALIDATED | Before the fix, the one-target fixture passed `G` (the first character of the cache path) to `Remove-LauncherPath`, removed 0 items, and left all cache files; see [maintenance-harness-before-fix.json](maintenance-harness-before-fix.json). The launcher now wraps the target pipeline in an array subexpression before indexing ([start_on_windows.ps1](../../../../start_on_windows.ps1:2490)). The rerun passed the one-target case: it removed 4 synthetic entries, preserved root/nested `.gitkeep`, and repeated with 0 removals. The three-target case passed with the runtime cache plus two existing legacy roots: it removed 7 synthetic entries, preserved the runtime root `.gitkeep`, removed both legacy roots, and repeated as a one-target no-op. The real CLI cancellation still exited 0 with “Operation cancelled. No changes were made.” All positive clearing used fixture paths under this evidence directory. |
| RemoveDesktopRelease | PASS at function level; real CLI cancellation passed | The real CLI cancellation exited 0 without changes. The source-extracted action and actual path-removal helper were run with release/staging/PyInstaller/CPU-overlay/Cargo/UI/generated-config targets redirected into a disposable fixture. All listed targets and the generated config were removed, an outside sibling marker remained, and a repeat was idempotent. The real canonical cleanup action was not approved against its existing Tauri target directory. |
| KillProcesses | PARTIAL | The source-extracted selector chose only a synthetic repo-scoped backend and its child. The actual stop function called an intercepted taskkill.exe stub with /PID 2000000000 /T /F; a second scenario with only an unrelated synthetic process made no kill request. Port inspection/termination was stubbed, so this does not prove an actual Windows process-tree stop. The direct action was not run because the coordinating task owned the live 5003/8003 application processes. |

The function-level harness and passing rerun are [maintenance-harness.ps1](maintenance-harness.ps1) and [maintenance-harness-results.json](maintenance-harness-results.json). Its disposable harness fixture was removed. The direct data-action fixture was retained temporarily for evidence capture and then removed from this directory.
