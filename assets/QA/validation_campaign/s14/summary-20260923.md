# S14 PostgreSQL Persistence Summary

Date: 2026-09-23  
Result: `PASS`

## Revision And Environment

- Application, test, and validation revision: `f6ee0a9dacf4b024cf2b8cea75c6f025c2dc7d63` (`develop`).
- The hosted workflow checked out a clean commit. The summary and ledger edits were made after this validation and are documentation-only.
- Hosted environment: GitHub Actions `ubuntu-latest`, Python 3.14.7, PostgreSQL 16 service, SQLAlchemy PostgreSQL driver `psycopg`.
- The PostgreSQL tests created uniquely named disposable databases through the workflow's `xreport` test role and dropped them during fixture cleanup.
- Local Windows PostgreSQL on `127.0.0.1:5432` was unavailable, so live PostgreSQL evidence comes from the hosted service.

## Scenarios And Results

The PostgreSQL integration command was:

```bash
app/server/.venv/bin/python -m pytest -c app/server/pyproject.toml app/tests/integration -q --basetemp runtimes/cache/pytest-tmp/integration -o "cache_dir=runtimes/cache/pytest"
```

It completed with `7 passed` in 2.17 seconds. The S14 scenarios were:

| Scenario | Result | Evidence |
| --- | --- | --- |
| `test_postgresql_migrations_and_restart_persistence` | `PASS` | The application initializer created a fresh database and Alembic reached `e91a4f6c2d73`. The schema contract and `SELECT 1` passed. `global_seed=2026092301` remained stored after disposing the database engine and running initialization again. |
| `test_postgresql_concurrent_initializers_use_advisory_locks` | `PASS` | Two concurrent initializers targeted the same new database. Both completed, and the resulting database had one Alembic head and the application settings table. This exercises database-creation and migration advisory locks. |
| `test_postgresql_connection_failure_does_not_leak_credentials` | `PASS` | A real connection refusal at `127.0.0.1:1` returned the actionable startup error without exposing the configured password in the exception or captured logs. |

The full hosted CI run [35841152401](https://github.com/CTCycle/XREPORT-radiological-reports-generator/actions/runs/35841152401) passed. Its adjacent gates included Ruff, `140` backend unit tests, Pyright (`0 errors`), retained API E2E (`9 passed`), frontend build/server checks, client lint, and client unit tests.

## Failure And Fix

The first hosted run, [35840303407](https://github.com/CTCycle/XREPORT-radiological-reports-generator/actions/runs/35840303407), reached Alembic head but failed the PostgreSQL schema-drift comparison. PostgreSQL's reflected check expressions included casts and `ANY(ARRAY[…])`; the normalizer removed adjacent `AND` expressions, and reflected indexes backing unique constraints were counted again as ordinary indexes.

The comparison now strips recognized PostgreSQL type casts without consuming neighboring SQL, normalizes PostgreSQL array membership expressions, and omits indexes already represented as unique constraints. Unit regressions cover the cast forms and duplicate-index reflection. The subsequent full hosted run passed.

## Windows Recheck And Scope

- Backend unit suite: `140 passed`.
- Ruff on the changed Python files: passed.
- Pyright from `app/server`: `0 errors, 0 warnings, 0 informations`.
- Local integration collection with the PostgreSQL opt-in disabled: `4 passed, 3 skipped`; the PostgreSQL cases were exercised by hosted CI instead.
- Windows pytest emitted the pre-existing `WinError 183` cache-path warning tracked by `ISSUE-004`.
- This result covers the PostgreSQL 16 persistence contract. It does not cover alternate PostgreSQL versions, production database credentials, or database performance under application workloads.
