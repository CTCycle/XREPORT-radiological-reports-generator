# S20 — Dataset Upload Parsing and Identity

**Status: PASS** for the upload parsing, size-limit, and upload-identity slice.
This does not validate the complete dataset preparation workflow.

## Revision and environment

- Source and test revision: `37a4b78e6ca939f8a2b6cb9e29c8fa7d46a3d41e` on `develop`.
- The evidence and ledgers were pushed in docs-only revision
  `16794b40948badfb1af4e4705048d1e6a56e5376`.
- The tracked working tree was clean before the focused run. Runtime-generated
  Python bytecode was removed after the run.
- Windows 11 Pro, build 26200; AMD Ryzen 5 5600H; Python 3.14.7.
- Source backend with isolated SQLite resources at
  `runtimes/cache/s20-resources`; no inference runtime or device was used.
- XLSX dependencies: `openpyxl==3.1.5`, `defusedxml==0.7.1`;
  `openpyxl.xml.DEFUSEDXML` was `True`.

## Scenarios and results

| Scenario | Result | Evidence |
| --- | --- | --- |
| Semicolon CSV with UTF-8 text parses with the expected rows and columns. | PASS | `test_upload_parses_semicolon_csv_and_preserves_utf8_text` |
| Comma CSV parses with the expected rows and columns. | PASS | `test_upload_parses_comma_csv` |
| Generated XLSX workbook parses with the expected rows and columns. | PASS | `test_upload_parses_xlsx_workbook` |
| Empty CSV and corrupt XLSX return HTTP 400. | PASS | `test_upload_rejects_empty_or_corrupt_workbooks` |
| Unsupported file types return HTTP 400. | PASS | `test_upload_rejects_unsupported_file_types` |
| A payload one byte over 16 MiB returns HTTP 413; exactly 16 MiB reaches the parser and succeeds. | PASS | `test_upload_rejects_payload_over_16_mib`; service boundary tests |
| Two API uploads receive distinct IDs; service state retains each filename and parsed contents under its own ID. | PASS | `test_upload_assigns_independent_ids`; `test_upload_ids_keep_parsed_contents_independent` |
| An unknown explicit upload ID is rejected. | PASS | `test_load_dataset_requires_the_explicit_upload_id` |

## Checks

- Windows test runner, targeted API and unit cases: **12 passed**.
- Ruff on the changed Python service and tests: **passed**.
- `git diff --check`: **passed**.
- `uv lock --check` with both the system and repository-bundled uv: **passed**.
- The API test server used port `5003` and the isolated SQLite root above. The
  runner's live-server and frontend phases were skipped because the backend was
  started and stopped as a tracked session; port `5003` was confirmed clear.

## Scope and omissions

- The CSV/XLSX parsing and explicit upload identity contract is covered for
  synthetic fixtures. No patient data or image files were used.
- This slice does not exercise image matching, partial-import confirmation,
  dataset processing, persisted metadata, or the rendered Dataset workflow.
  Keep `workflow.dataset_upload_and_preparation` `UNVALIDATED` until those later
  boundaries are exercised.
- Hosted CI run `35859367232` passed all configured gates on pushed revision
  `16794b40948badfb1af4e4705048d1e6a56e5376`, a docs/evidence-only descendant
  of source/test revision `37a4b78`, after its failed-job rerun.
- The latest hosted run `35860925179` on ledger/evidence revision
  `5cbe2a7c86ec828ff8d9cecd35a55b94f9e9d0e3` passed all backend, API, and
  client build gates but failed the client unit stage; its same-SHA rerun
  repeated two `desktop-dialog.service.spec.ts` assertions. This gate is outside
  S20's upload scope. S20's local focused suite and hosted API E2E stage passed;
  keep S20 `PASS` and the broader Tier 0 baseline `PARTIAL`.
  [Current CI run](https://github.com/CTCycle/XREPORT-radiological-reports-generator/actions/runs/35860925179)
