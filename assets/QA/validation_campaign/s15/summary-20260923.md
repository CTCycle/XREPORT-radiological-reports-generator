# S15 — Local Filesystem Access and Path Controls

**Status: PASS for the filesystem access and folder selection slice.** The
broader dataset upload and preparation workflow remains unvalidated.

## Revision and environment

- Application source revision under validation: `0ace867ea8087a112305bb9acce1770f259e2f2c` on `develop`.
- The source application required no changes. This validation change set adds
  focused API and UI regressions, this evidence summary, screenshots, and ledger
  updates.
- Windows 11 Pro, build 26200; official `start_on_windows.ps1 -Action Launch`
  launcher.
- The launcher used backend port `5015`, UI port `8015`, and isolated resources
  root `runtimes/cache/s15-resources`. The original ignored `settings/.env` was
  restored byte-for-byte after launch. The pre-existing UI service on port
  `8003` was left untouched.
- The rendered E2E generated isolated per-test fixtures: one one-pixel PNG
  image, an empty directory, a missing path, and a file path used where a
  directory was expected. The manual in-app-browser run used synthetic fixtures
  beneath `runtimes/cache/s15-browser-fixtures`.

## Scenarios and results

| Scenario | Result | Evidence |
| --- | --- | --- |
| API browse reports the accessible image and empty folders and image counts. | PASS | `test_s15_filesystem_browse_validation_and_feature_gate` |
| API image validation accepts the image folder, rejects the empty folder, and returns invalid for missing/file paths. | PASS | `test_s15_filesystem_browse_validation_and_feature_gate` |
| API browse and image validation reject requests while filesystem access is disabled. | PASS | `test_s15_filesystem_browse_validation_and_feature_gate` |
| Dataset browse control is disabled with its server-configuration explanation when filesystem access is disabled. | PASS | [Disabled browse screenshot](s15-disabled-browse.png) |
| Rendered browse reports an invalid path, then recovers after navigating to a valid directory. | PASS | [Invalid path screenshot](s15-invalid-path.png); [successful selection screenshot](s15-image-selected.png) |
| Empty-folder selection shows “No valid images found” and leaves the folder picker open. | PASS | [Empty folder screenshot](s15-empty-folder.png) |
| Valid image-folder selection closes the picker and displays a count of one image. | PASS | [Image selected screenshot](s15-image-selected.png) |
| The browser-mode UI workflow was exercised with the official launcher, isolated resources root, and synthetic fixtures. | PASS | Rendered browser E2E plus the four screenshots above |

The API test restores the original filesystem-access setting in a `finally`
block. The browser E2E also restores it in a `finally` block. The manual
in-app-browser run confirmed the same visible disabled, invalid, recovery,
empty, and successful-selection states against the isolated launcher.

## Checks

- Focused backend API and rendered browser tests: **2 passed**.
- Angular unit suite: **38 passed** across 12 spec files.
- Ruff on the two new Python E2E files: **passed**.
- `git diff --check`: **passed** on the final staged change set.

## Scope and omissions

- The exercised path policy is an accessible existing directory while the
  filesystem feature is enabled. No new path allowlist was introduced or
  tested.
- Browser-mode server folder selection was used; the Tauri native picker was
  not exercised.
- This evidence does not cover CSV/XLSX upload, image-to-row matching,
  unmatched-row confirmation, processing, persistence, or a downstream-ready
  dataset. Keep `workflow.dataset_upload_and_preparation` **UNVALIDATED**.
- No public API or schema change was made.
