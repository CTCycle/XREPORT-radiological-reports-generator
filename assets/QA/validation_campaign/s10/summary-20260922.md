# S10 validation summary — 2026-09-22

## Scope and evidence boundary

S10 covers routing, navigation state, theme preference resolution and persistence, guidance persistence/replay, direct loading and refresh, and browser console/request cleanliness. This is software and runtime validation for research use; it does not establish clinical validity.

- Branch: `develop`
- Source checkout HEAD at execution: `23f56f489260367db83e16e5925e1a562d175779`
- Working-tree changes during this campaign: `app/tests/e2e/test_angular_ui.py` (S10 coverage and configurable screenshot staging) and `start_on_windows.ps1` (Windows PowerShell-compatible relative paths and ordinal fingerprint ordering).
- `app/client/src` and its build configuration had no source diff from the Tier 0 revision `3f180d229278aff379358a9f8d3eddf3b07dee5c`. The served bundle was the current ignored bundle with `.xreport-build-state.json` production fingerprint `3ad40dd21dec9dea81e4e6701d6da5a787b96913bd8a2a5db09bbd85d815e194`, dependency fingerprint `b8cb6a4489e064c301d508c16b14af10e3afa1b2102f3b2104a23d55d8b7d6d0`, and Node `22.22.3`.
- Host: Windows; Windows PowerShell 5.1 and PowerShell 7.6.6; in-app Codex browser; local FastAPI on 5003 and built frontend/proxy on 8003.

## Scenario matrix

| Area | Exercised behavior | Outcome |
| --- | --- | --- |
| Redirect | Direct `/` load redirects to `/inference`; inference shell is usable. | PASS |
| Routed surfaces | Direct load and browser refresh for `/inference`, `/reports`, a disposable seeded `/reports/:requestId`, `/dataset`, `/training`, disposable `/dataset/validate/:datasetName`, and `/settings`. | PASS |
| Navigation | Sidebar traversal through Inference, Reports, Dataset, Training, and Settings; exactly one active link at each destination. Reports remains active for report detail; Dataset remains active for dataset validation. | PASS |
| Theme | Light and Dark selected states (`aria-pressed`), `data-theme`, localStorage persistence, reload persistence, and manual preference stability across OS theme changes. | PASS |
| System theme | System preference follows emulated light/dark `prefers-color-scheme`, persists across navigation/reload, and changes resolved `data-theme` without changing the selected System preference. | PASS |
| Guidance | Version-0 stored dismissal is re-shown as version 1; first-use tip records `seen`, dismiss records `dismissed`, tour close records `skipped`, tour Finish records `completed`; reload suppresses completed/dismissed content; Help and tips manually replays the tour. | PASS |
| Cleanliness | S10 collectors recorded no unexpected console errors, page errors, or failed requests. | PASS |

The valid report-detail fixture was created and deleted by the existing disposable `seeded_history` fixture. The dataset-validation route used a deterministic browser-scoped response for `e2e-s10-validation-route-fixture`, so no real dataset or persisted report was created.

## Commands and results

- `npm --prefix app/client run test:unit`: **36 passed**.
- `npm --prefix app/client run lint`: **passed**.
- `npm --prefix app/client run test:built-server`: **9 passed**.
- Focused S10 run: `pytest ... app/tests/e2e/test_angular_ui.py -k s10_`: **3 passed**.
- Full Angular E2E module: **13 passed**, with one pre-existing pytest cache ACL warning.
- Official `app/scripts/run_angular_e2e.ps1` wrapper: **13 passed**. The wrapper required elevated filesystem access only to create its prescribed disposable `runtimes/cache/pytest-tmp` path.
- `npm --prefix app/client run build` and direct Angular production/development builds exited with native Windows status `-1073741819` immediately after `Building...` on system Node `22.23.1`. No client production source changed during S10; the current previously built bundle was used for the browser gate. This build/toolchain limitation remains open and is not claimed as a build PASS.
- Windows PowerShell `start_on_windows.ps1 -Action Launch` reached `Current` build reuse, started the backend and frontend, and reached UI health. Its final automatic `Start-Process` browser-open step returned managed-environment `Access denied`; the same URL was opened manually in the Codex in-app browser. Backend and proxy health both returned HTTP 200. Only the verified XREPORT PIDs on ports 5003 and 8003 were stopped afterward; no listeners remained.

## Durable visual evidence

Representative screenshots are in this directory:

- [route-inference.png](route-inference.png)
- [route-report-detail.png](route-report-detail.png)
- [theme-light-inference.png](theme-light-inference.png)
- [theme-system-dark-settings.png](theme-system-dark-settings.png)
- [guidance-first-use-tip.png](guidance-first-use-tip.png)
- [guidance-tour-completion.png](guidance-tour-completion.png)
- [xreport-startup-gate.png](xreport-startup-gate.png)
- Responsive route captures: `xreport-inference-narrow-dark.png`, `xreport-dataset-narrow-dark.png`, `xreport-settings-narrow-dark.png`, and `xreport-settings-narrow-light.png`.

## S10 decision

All required S10 route, refresh, navigation, theme, guidance, persistence, and cleanliness scenarios passed. S10 is **PASS** for this revision and environment. The Angular build native crash, protected cache ACL warnings, and launcher browser-open limitation remain explicitly scoped adjacent issues; they do not invalidate the observed S10 behavior of the current built bundle.
