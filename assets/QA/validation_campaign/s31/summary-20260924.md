# S31 Public-Model Installation Lifecycle Validation

- Date: 2026-09-24
- Code baseline: `80e8d7aea3530c31d7557445685591533a27b3c5` (`develop`)
- Result: **PASS** for the lightweight public-model lifecycle and technical inference scope described below.

## Environment and fixture

- Windows 11 build 26200; Python 3.14.7; PyTorch 2.10.0+cu130; NVIDIA GeForce RTX 3060 Laptop GPU (6 GiB); CUDA available.
- Official `start_on_windows.ps1` launcher, with disposable resource root `runtimes/cache/s31-20260924/resources`, isolated SQLite state, and the local API/UI on ports 5003/8003. The app was stopped through the official launcher after checks; both ports were confirmed clear.
- Public open model `aehrc/cxrmate-multi-tf`, exact revision `330721b9aa5bba201a3eb88eba4dd9a6607f3e7a` (Apache-2.0, research use).
- One public de-identified research image: `fixture-covid-chest-xray.jpg`, SHA-256 `4570a9524d57cb2697ea577ac5d36553cf28e0d190d347edda1fe12f07d54751`, from `ieee8023/covid-chestxray-dataset`, file `000001-1.jpg`. Its evidence states that no direct identifier is included and use was local.

## Executed scenarios

1. **Fresh download:** maintenance job `21947e60` downloaded the exact pinned revision in 60.36 seconds into the isolated staging area. The candidate passed snapshot integrity verification and was correctly left `staged`/`unvalidated` pending inference.
2. **Repair:** one byte was appended to the disposable staged `config.json`. Repair job `9aa11b78` completed; only `config.json` changed, and its original 78,452-byte content and SHA-256 were restored exactly. The model weights were not modified.
3. **Readiness promotion:** inference job `4b22a07d` completed in 7.32 seconds on CUDA (`cuda:0`, float32), producing non-empty Findings and Impression for one image with the exact model revision in provenance. The isolated catalogue moved to `ready`/`active`, with verified snapshot integrity.
4. **Restart reuse:** after a controlled official-launcher stop and restart, the backend recovered and the same active path and pinned revision were reused. A second real inference completed in 9.01 seconds. The eight-file, 450,952,998-byte snapshot inventory and hashes were unchanged; no new maintenance/download operation was issued.
5. **Deletion and preservation:** `delete_local` job `c85f06c5` completed in 0.8 seconds and reported 452,386,829 bytes freed. The disposable snapshot and metadata were removed; the five-entry public catalogue remained and the model returned to `not_installed`; no custom model was present or altered. Both successful generation-history records persisted across deletion. The delete also removed the revision-scoped Transformers dynamic-module cache entry; an offline load of the untouched canonical `app/resources` snapshot at the same pinned revision then succeeded and recreated that cache entry.

## Evidence

- [Fresh download job](download-job.json), [staged catalogue](catalog-staged.json), and [staged snapshot manifest](snapshot-manifest.json).
- [Corruption injection](repair-injection.json), [repair result](repair-job.json), and [post-repair catalogue](catalog-corrupt-candidate.json).
- [Fixture provenance and hash](fixture-evidence.json), [first real inference](real-inference-job.json), and [catalogue before inference](catalog-before-inference.json).
- [Active snapshot before restart](snapshot-before-restart.json), [manifest comparison and second inference](restart-reuse.json), [delete result and history check](delete-job.json), and [offline canonical snapshot reload](canonical-snapshot-reload.json).
- [S28/S35/S37 prerequisite recheck](prerequisite-recheck-20260924.json).

## Boundary and remaining work

S31 passes for one open pinned model, one image, real API generation, installation repair/reuse/deletion, and technical readiness promotion. This is not multi-case quality, clinical-performance, diagnostic-accuracy, or general provider validation. Only a one-image study was exercised; multi-view behavior was not evaluated. The rendered browser Generate/review workflow remains `S33 UNTESTED`; its upload, cancellation, retry, editing, copying, export, provenance display, and browser-visible persistence states were not exercised in this slice. S28 remains `UNTESTED` without its pinned encoder and compatible S24 checkpoint. S35 remains `FAIL / degraded` because its approved fixture directory is missing and the historical three cases produced two distinct reports. S37 remains `BLOCKED` without configured authorized gated access. Those statuses were rechecked and retained; see the prerequisite evidence and campaign ledger.
