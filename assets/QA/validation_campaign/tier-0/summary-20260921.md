# XREPORT Tier 0 Validation Summary

Date: 2026-09-21
Code revision: `481605b1b87035b8deb03edaefdbfc090f8f1b23`
Branch: `develop`
Environment: Windows, Python 3.14.7, Node.js 22.23.1, npm 10.9.8, SQLite,
repository `app/server/.venv`
Scope: foundational health only; no dataset, training, provider-quality, or
desktop-release claims

## Outcome

**Tier 0 is not cleared.** S01, S02, and S03 passed in the local Windows
environment. S00 remains `FAIL` because the current remote CI result for this
code revision is red, even though the named test passed locally ten consecutive
times and the complete local backend unit suite passed. No implementation fix
was applied without the full remote traceback.

Remote baseline evidence supplied by the repository inspection:

- GitHub Actions run `35598132452` on this revision: backend unit stage failed
  with `131 passed, 1 failed` in
  `test_concurrent_service_initialization_keeps_ml_imports_lazy`; later
  Pyright, PostgreSQL, API E2E, frontend build, lint, and frontend unit stages
  were skipped.
- Last verified fully green baseline: run `35348243935`, commit
  `5d469126385f1f414023d91845a8c8a317702d3f`, before the current import/startup
  changes.

## S00 — Current CI Baseline

Commands and observations:

```powershell
$env:PYTHONPATH = "app"
$env:KERAS_BACKEND = "torch"
$env:MPLBACKEND = "Agg"
$env:PYTHONDONTWRITEBYTECODE = "1"
app\server\.venv\Scripts\python.exe -m pytest -c app/server/pyproject.toml app/tests/unit/test_ml_import_boundaries.py::test_concurrent_service_initialization_keeps_ml_imports_lazy -q --basetemp runtimes/cache/pytest-tmp/tier0-s00-single -o "cache_dir=runtimes/cache/pytest"
app\server\.venv\Scripts\python.exe -m pytest -c app/server/pyproject.toml app/tests/unit -q --basetemp runtimes/cache/pytest-tmp/tier0-s00-unit -o "cache_dir=runtimes/cache/pytest"
```

Results:

- Named test: first run passed; ten-repeat campaign passed `10/10`.
- Complete backend unit suite: `132 passed in 37.63s`.
- Classification remains unresolved across environments. The next S00 action
  is to capture the complete CI subprocess traceback, determine whether the
  failure is a backend, persistence/state, configuration, or test/environment
  defect, then rerun the failed test at least ten times and the skipped CI
  stages.

## S01 — Source Backend and SQLite Readiness

The backend was started twice against a fresh disposable resource root:

`runtimes/cache/validation-tier0-s01-20260921`

Fresh and restart health responses were both:

```json
{
  "status": "ok",
  "application": "XREPORT Backend",
  "version": "3.1.0",
  "runtime_mode": "sqlite",
  "runtime_variant": null,
  "runtime_port": 5013
}
```

The SQLite `alembic_version` row was `f48a7c2e91b6` after fresh startup and
after restart. The restart reused the existing database/resource root.

For the fail-closed scenario, a copy of the populated database had its
`alembic_version` table removed. Startup exited with code `3`, returned no
health response, and logged:

> Non-empty database has no Alembic revision; refusing implicit schema adoption.

This proves the current startup boundary for fresh, repeatable, and
unversioned-schema cases. The disposable resource roots are not application
data and must remain outside the tracked evidence.

## S02 — Angular Startup Gate and Recovery

The official command was invoked:

```powershell
.\start_on_windows.ps1 -Action Launch
```

It reported environments ready, started backend/frontend services, and reached
frontend readiness. Its final attempt to open the URL through Windows
`Start-Process` returned access denied in the managed environment. The same
live URL was opened through the Codex in-app browser; `/inference` rendered the
ready shell and the model catalogue after backend health became available.

The focused rendered test passed:

```text
app/tests/e2e/test_angular_ui.py::test_startup_gate_holds_inference_until_backend_health
1 passed in 5.87s
```

The test simulated two health `503` responses followed by `200`, verified the
startup surface stayed visible, confirmed the inference component and model
catalogue were not instantiated before readiness, and then observed recovery.
It also refreshed the tracked screenshot [`xreport-startup-gate.png`](../../xreport-startup-gate.png).

Explicit slow/unavailable/retry timing and post-ready ordinary feature-error
coverage remain open for the next S02 evidence pass; existing unit coverage is
not promoted to manual workflow proof here.

## S03 — Static, Contract, and Client Gates

Results on the same code revision:

| Gate | Result |
| --- | --- |
| `uv sync --locked --extra test --python 3.14.7` | PASS; locked environment resolved successfully. |
| Ruff | PASS; one Windows access-denied cache warning was emitted and retained as an infrastructure note. |
| Pyright | PASS; `0 errors, 0 warnings, 0 informations`. |
| Backend unit suite | PASS; `132 passed in 37.63s`. |
| `npm ci --no-audit --no-fund` | PASS after cleanup; initial attempt hit `EPERM` unlinking a launch-held `@esbuild/win32-x64/esbuild.exe`. |
| Angular production build | PASS; application bundle generated in `14.794s`. |
| Angular lint | PASS; all files passed. |
| Angular unit suite | PASS; `8` test files and `29` tests passed. |

The first `npm ci` failure was resolved by verifying and stopping only the
XREPORT launcher-owned frontend tree. The exact process roots were then
terminated, and ports `5003` and `8003` were confirmed clear. The official
`KillProcesses` action itself could not inspect processes because of the same
managed Windows access boundary; that limitation is recorded rather than
silently treated as a clean launcher result.

## Tier 0 Next Action

Do not advance to S10 or the dataset campaign as if the foundation were green.
Reproduce the remote S00 failure with complete stderr/traceback, classify the
cause, make the smallest owner-level correction if required, run the S00
regression set, and obtain a green current-head CI run. Then repeat S01–S03 on
that exact revision and update both ledgers.
