# Tier 0 refresh — 2026-09-24

## Scope and gate selection

The validation-ledger review found S00 and S03 `PASS`; S01 `PASS` for its
recorded source-startup scope while `runtime.source.startup` remains `PARTIAL`;
and S02 `PASS` with explicit timing and post-ready error evidence previously
open. This refresh selected the related source-launch and rendered-readiness
work together. It closes the recorded S02 gap and validates the noninteractive
occupied-port refusal path for S01. The remaining S01 interactive ownership
races and dependency-invalidation cases are not small extensions of this run
and remain listed as open.

The broader ledger remains mixed: Tier 1 S10 and S12–S15 are `PASS` (S11 is
`VALIDATED` for its exercised surface); Tier 2 S20–S27 are `PASS` and S28 is
`UNTESTED` because its pinned encoder/checkpoint prerequisites are absent;
Tier 3 S30–S32 are `PARTIAL`, S33–S34 are `UNTESTED`, and S35 is `FAIL` /
degraded; Tier 4 S36A/S36B/S40/S43 are `PARTIAL`, S37 is `BLOCKED`, and
S41/S42 are `UNTESTED`; Tier 5 S50/S51 are `UNTESTED`, S52 is `PARTIAL`, and
S53 is `UNKNOWN`. These unrelated gates were not promoted by this refresh.

## Revision and environment

- The working tree was based on `develop` revision
  `e85ecbfbb20a062c119af0994f37cc1033c4ca20`; the launcher, focused E2E, and
  this report contain the reviewed validation changes made during this run.
- Windows 11 `10.0.26200`; Python `3.14.7`; Node.js `22.22.3`; SQLite.
- Official source launch used a disposable root at
  `runtimes/cache/s02-20260924-startup-gates/resources` through the process
  variable `XREPORT_RESOURCES_DIR`. The launcher now preserves this override
  when loading `settings/.env`.
- The default `app/resources/database.db` remained unchanged (modified
  `2026-09-23 09:03:46`, 159744 bytes). No model download, installation, or
  generation was started.

## S01 — source launcher and SQLite readiness

- The official `start_on_windows.ps1 -Action Launch` passed preflight, reused
  the built frontend, started FastAPI and the Node UI server, and returned
  HTTP 200 from both `/api/health` and `/`. The isolated SQLite database
  reached Alembic head `e91a4f6c2d73`; `PRAGMA integrity_check` returned `ok`.
- Windows denied automatic browser opening with `Access is denied`. The
  launcher now catches this after service readiness, emits a warning with the
  manual UI URL, and leaves both services running. The in-app browser rendered
  the ready inference workspace and public catalogue.
- A second noninteractive Launch was attempted while the first instance was
  running. `Get-NetTCPConnection` returned access denied, so the new
  `netstat.exe` fallback found listeners on ports 5003 and 8003 and their
  PIDs. Process metadata lookup was also denied; the launcher listed PIDs and
  ports and exited nonzero with an explicit no-termination message. Follow-up
  health checks confirmed the original instance remained available.
- The existing `KillProcesses` action was attempted but Windows denied its
  WMI process-table query. Only the exact PIDs started by this validation were
  subsequently stopped after verifying executable paths and launch times.
- **Result:** the exercised fresh-root startup and noninteractive conflict
  refusal passed. The campaign S01 row remains `PASS` at that scope;
  `runtime.source.startup` remains `PARTIAL`. Permission-denied termination,
  post-consent PID takeover, still-bound owner, output deletion,
  rebuild-after-source-change, package-lock/npm-ci recovery, and
  edit-during-build still need end-to-end validation.

## S02 — rendered startup and recovery

- Commands:
  - `pytest -c app/server/pyproject.toml app/tests/e2e/test_angular_ui.py::test_startup_gate_holds_inference_until_backend_health app/tests/e2e/test_angular_ui.py::test_startup_timing_retry_and_post_ready_feature_error -q --basetemp runtimes/cache/pytest-tmp/tier0-s02-20260924 -o "cache_dir=runtimes/cache/pytest"`
  - `npm run test:unit` from `app/client`.
  - `ruff check app/tests/e2e/test_angular_ui.py`.
- The focused Playwright run passed (`2 passed in 5.69s`); the client unit
  suite passed (`12` files, `40` tests); Ruff passed. The readiness test
  verified routed content and catalogue requests remain suppressed before
  health succeeds. The new case advanced Playwright's clock to render the
  slow message at 15 seconds and unavailable/retry state at 60 seconds, then
  enabled health and verified recovery. A synthetic model-catalogue 503 after
  readiness stayed inline in the inference route; the startup screen did not
  return.
- The controlled clock and intercepted health/catalogue endpoints provide
  deterministic state-transition coverage, not a wall-clock service outage.
  Packaged CPU/CUDA startup and real inference were not exercised.
- **Result:** S02 `PASS` at this scope; `ui.startup_gate` remains
  `VALIDATED`, with packaged desktop validation still open.

## Visual evidence

The five PNGs under [S02 captures](s02-20260924/) were inspected visually.
They show the loading gate, slow message, retry state, ready inference
workspace, and inline post-ready feature error. All are 1024 × 720.

| Capture | SHA-256 |
| --- | --- |
| `xreport-startup-gate.png` | `02913344B8C9F566D74016E211BBB0478A1E7812840E8A21BF2112C064139BAA` |
| `startup-slow.png` | `3B07D7DC0CEA4E418C203AD0CA7C540B17F822C7788BA1776DFF35E02E36151A` |
| `startup-unavailable.png` | `45D96E28782B6EE70964EBB5BC3691712A77CF12BEBB70F2EE2B38DA3BADCEA7` |
| `xreport-startup-ready.png` | `19F8F2CDFDA890A3E0B5356E854E6F1D33A8FFF16D9B2765E4D4EA3D5238E362` |
| `startup-feature-error-after-ready.png` | `4962CE30DD080ED858AC9C9DB12210684F95B4BA25F24FE4344778A10676370A` |

## Remaining Tier 0 work

The next actionable slice remains the S01 launcher ownership and dependency
invalidation cases: termination refusal/failure and ownership races, still-
bound port detection, build-output deletion, source-change rebuild,
package-lock/npm-ci recovery, and edit-during-build. Run those in a disposable
checkout state. Keep `runtime.source.startup` `PARTIAL` until they are
exercised. S28 remains independently untestable until the exact pinned BEiT
encoder is restored and a compatible S24 checkpoint is recreated.
