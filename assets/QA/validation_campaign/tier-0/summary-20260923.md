# Tier 0 refresh — 2026-09-23

## Scope and revision

This refresh re-ran the S02 startup gate against the current `develop` checkout.
The application source revision at test time was `fdb4a8bf9af37eadea648851cb1a0b54e187f7b3`; the focused E2E assertion and ready-state capture in `app/tests/e2e/test_angular_ui.py` were local changes that are included with this evidence. S02 passed. This report does not close the still-open S01 launcher edge cases or the remaining S02 recovery scenarios.

## Environment and execution

- Windows 11 Pro 10.0.26200; Python 3.14.7; Node.js 22.22.3.
- Codex in-app browser opened the built frontend at `http://127.0.0.1:8003/inference`; the E2E test used the configured backend URL `http://127.0.0.1:5003` and intercepted health responses to deterministically exercise readiness.
- Focused scenario: `app/tests/e2e/test_angular_ui.py::test_startup_gate_holds_inference_until_backend_health`.
- Result: `1 passed in 4.20s` using `app/server/.venv/Scripts/python.exe`, a task-specific basetemp under `runtimes/cache/pytest-tmp/tier0-s02-20260923-push-check`, and repository-local pytest cache `runtimes/cache/pytest`.
- Health polling returned two `503` responses followed by `200`. Before readiness, the test confirmed the inference page and model catalogue were absent and no model-list request had been sent. It confirmed every startup radiograph had the expected source, empty alt text, `aria-hidden`, and a decoded image, then waited for the model catalogue and captured the ready workspace. The browser console had no warnings or errors.
- No launcher build refresh was attempted in this run. The currently served startup screen rendered the updated design, and `startup-radiograph.png` matched the current source asset (SHA-256 `473F83B56FE9601642F3822A9FDDDB863B1FAFFF5A8F786334DC5E203EC5E00A`).

## Visual evidence

Both captures are 1024 × 720 PNGs and were inspected in the in-app browser:

- [Loading gate](s02-20260923/xreport-startup-gate.png) — SHA-256 `E989B4D29102535C79C18FDD48DB09A0CDDB18B52E3D31971C8D792B30569298`.
- [Ready workspace](s02-20260923/xreport-startup-ready.png) — SHA-256 `72D24F903D2BF023E1B6371C381BE26BE919AFC7B29E65602C9FE8A03BACA5DF`.

The loading capture shows the current startup artwork and styling; the ready capture shows the model catalogue after health recovery.

## Boundaries

This scenario proves the deterministic loading-to-ready gate and current rendered appearance. Explicit slow-backend timing, sustained unavailability, retry action, and an ordinary post-ready feature error remain untested. The official launcher/build refresh was not repeated: its preflight saw the existing XREPORT listeners on ports 5003 and 8003 and declined to start another instance. Those pre-existing processes were left untouched. The served screen and startup asset were inspected directly.

The parent revision had passed hosted CI run [35829720104](https://github.com/CTCycle/XREPORT-radiological-reports-generator/actions/runs/35829720104). The task commit’s hosted CI result is reported in the completion note after push. The task-specific pytest basetemp was removed after validation; the screenshots above are retained.
