# Tier 0 execution summary — 2026-09-22

Initial launch timing and port-preflight evidence below came from a Windows
`develop` working tree based on `481605b1b87035b8deb03edaefdbfc090f8f1b23`,
before the startup implementation was committed; the working-tree source was
therefore not identified by that base SHA alone. A separate normal-launch and
rendered-readiness check was repeated from committed code/test revision
`3f180d229278aff379358a9f8d3eddf3b07dee5c`, recorded below. Environment:
project Python 3.14.7 and portable Node.js 22.22.3. This is software/runtime
evidence only; it does not establish clinical validity.

## S01 — Source launch and backend startup

- `app/tests/run_tests.bat`: live-service phase passed; Python suite passed
  (`149 passed, 1 skipped`), including database initialization and schema-drift
  tests. No migration or schema validation behavior was weakened.
- The repository-defined isolated backend unit command passed (`134 passed`).
- `start_on_windows.ps1 -Action RebuildFrontend` completed a production build
  with portable Node 22.22.3 and refreshed the ignored build-state manifest.
- A subsequent `-Action Launch` reported `Frontend build status: Current` and
  skipped Angular compilation. The final warm-run sample logged: port preflight
  664 ms, dependency readiness 937 ms, build freshness check 148 ms, no-op
  rebuild phase 4 ms, backend process start 67 ms, frontend process start
  16 ms, and UI reachability 607 ms. This is one warm-run sample; no pre-change
  timing baseline was captured, so it does not quantify a before/after speedup.
- The first launcher attempt before quoting the frontend script path failed its
  UI health check because Windows split a path containing spaces. Quoting that
  argument fixed the launch; the final invocation exited successfully.
- Port preflight checks exercised: both ports free; separate owners; one PID
  owning both ports; non-interactive fail-closed; declining without termination;
  approving and terminating once; and identical-port rejection. Permission-
  denied termination, a still-bound approved PID, and a new PID racing to bind
  after consent were not exercised end to end.

## S02 — Built frontend, proxy, and rendered readiness

- `npm run test:built-server`: 9/9 passed for root index, direct assets, Angular
  route fallback, API health/status, forwarded request bodies, controlled 502,
  encoded traversal rejection, and symlink targets outside the bundle.
- The rendered startup-gate E2E passed in the full runner. The in-app browser
  opened the launcher-served `http://127.0.0.1:8003/inference`, observed the
  startup shell, then the ready inference workspace and model catalogue
  (9 entries: 5 public, 4 custom).
- The full frontend E2E slice passed (7/7). Existing QA screenshots were
  refreshed by the responsive/rendered test run.

## S03 — Static and client gates

- Ruff: all checks passed (the process emitted an access-denied cache warning).
- Pyright: 0 errors, 0 warnings, 0 informations.
- Production Angular build passed with portable Node 22.22.3; the explicit
  launcher rebuild also completed and wrote current build state.
- Client lint passed; frontend unit tests passed (29 tests across 8 files).
- Startup settings tests passed: database preparation remained in sequence,
  server settings were composed once, and the returned settings populated app
  state and the health runtime mode.
- Focused fingerprint checks showed that a missing manifest triggered one
  rebuild/state write, a production stylesheet edit changed status to stale,
  restoring it returned status to current, and temporary spec-file creation did
  not invalidate the bundle. The missing-manifest helper run used the system
  npm executable; the later full launcher rebuild passed on portable Node.
- No `npm ci` was needed or run because dependency manifests were unchanged.
  Production-source status invalidation and spec-file exclusion were checked;
  the rebuild-after-source-edit, add/remove source, output deletion, package
  lock invalidation, and input mutation-during-build paths remain untested.

## Gate status and cleanup

S01–S03 passed for the stated local Windows scope. At the time this local
summary was first recorded, S00 remained **NOT CLEARED** because remote CI
`35598132452` was red. The S00 restoration evidence below supersedes that
interim status.

The launcher-owned backend and frontend process trees were stopped after the
browser check; ports 5003 and 8003 were confirmed free. Build output and the
generated `.xreport-build-state.json` remain ignored artifacts.

## S00 — Hosted baseline restoration (2026-09-22)

Validated revision: `3f180d229278aff379358a9f8d3eddf3b07dee5c` on `develop`.
The commit changes tests only; no production code changed.

### Diagnosis and regression coverage

- Diagnostic hosted run `35743413087` on `187aff1a6ee6d2dda5dfd46feae50644c371497f`
  exposed `sqlite3.OperationalError: no such table: application_settings`.
  The subprocess used the default resources path and queried the singleton row
  before the test initialized its schema; a clean hosted checkout had no
  migrated database there. No production synchronization defect was established
  by this traceback.
- `test_concurrent_service_initialization_keeps_ml_imports_lazy` now sets
  `EMBEDDED_DATABASE=true`, removes a developer `DATABASE_URL`, uses a dedicated
  `XREPORT_RESOURCES_DIR` under `tmp_path`, and runs the database initializer
  before concurrently constructing the services. It still launches ten fresh
  interpreters and checks the original lazy-ML module list and service results.
- The local built-server test observed `ECONNRESET` on Windows when the backend
  had just closed; Linux CI had expected `ECONNREFUSED`. Its assertion now checks
  the stable controlled 502 response and accepts either socket error code.

### Local Windows validation

- Backend unit suite, with repository-local pytest fixtures and an external
  temporary pytest cache:
  ```text
  app/server/.venv/Scripts/python.exe -B -m pytest -c app/server/pyproject.toml app/tests/unit -q --basetemp runtimes/cache/pytest-tmp/s00-unit -o "cache_dir=%TEMP%\xreport-s00-pytest-cache-elevated"
  Result: 135 passed in 34.40s.
  ```
- The local backend suite first failed when its temp root was outside the
  repository's allowed application roots (four model-storage path assertions);
  the intended repository-local pytest directory then required elevated access
  because of its ACL. The successful run above used that repository-local base
  directory. Temporary test caches were removed after the run.
- `app/server/.venv/Scripts/pyright.exe .` from `app/server`: `0 errors,
  0 warnings, 0 informations`.
- Ruff from `app/server`, `ruff check --no-cache .`: all checks passed; the
  process still warned that pre-existing cache directories were inaccessible.
- From `app/client`,
  `..\..\runtimes\nodejs\node.exe --test scripts/serve-built.test.cjs`:
  `9 passed, 0 failed`.

### Hosted CI evidence

- [CI run 35748390899](https://github.com/CTCycle/XREPORT-radiological-reports-generator/actions/runs/35748390899)
  completed successfully on the validated revision. Environment: Ubuntu 24.04,
  Python 3.14.7, uv 0.11.30, PostgreSQL 16, and Node.js 22.22.3.
- Backend unit tests: `135 passed`; Ruff: all checks passed; Pyright: `0
  errors`; PostgreSQL persistence contract: `1 passed`; retained API E2E: `9
  passed`; client build, built-server tests (`9`), lint, and client unit tests
  (`36`) all passed.
- This completes S00. The workflow also reran its automated backend unit,
  startup/API E2E, PostgreSQL contract, production build, built-server, lint,
  and client unit stages on this revision; it does not run the Windows source
  launcher or rendered startup-gate browser flow. The runtime source did not
  change after this revision.

### Same-revision source/browser recheck

- From the committed checkout at source/test SHA
  `3f180d229278aff379358a9f8d3eddf3b07dee5c`,
  `start_on_windows.ps1 -Action Launch` passed its
  port preflight, dependency readiness, and current-build checks, started the
  backend and built frontend, and reported the frontend URL. Its automatic
  `Start-Process` browser-open step then returned `Access denied` in this
  managed environment; this invocation therefore did not exit successfully.
- Opened that local URL in the Codex in-app browser. The inference workspace
  rendered ready, showed 9 models (5 public and 4 custom), and showed the
  selected CXRMate Multi TF model as `ready`, `active · verified`, with
  validation evidence `passed`. Browser console inspection found no warnings
  or errors. `/api/health` returned HTTP 200 on both ports 5003 and 8003.
- Closed the in-app browser tab and stopped only the two server processes
  started by this check after verifying their executable paths. Ports 5003 and
  8003 had no listening processes afterward. The rendered page was visually
  inspected in the browser; no new screenshot was saved, and the existing
  startup screenshot remains evidence of the initial loading shell only.
- This recheck confirms the observed source startup and rendered readiness on
  the pinned runtime revision while preserving the browser-launch limitation
  and the remaining S01/S02 edge cases above.

**Tier 0 exit:** S00, S01, S02, and S03 are green for their stated scopes.
The next open Tier 1 target is `S10`; later-tier work remains outside this
slice.
